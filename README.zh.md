# Astro MD Theme Monorepo

一个基于 Astro + Tailwind 的个人博客主题 Monorepo 项目。

## 📁 项目结构

这是一个 pnpm workspace monorepo，包含：

```
.
├── packages/
│   └── theme/          # 主题核心包（组件、样式、配置）
├── apps/
│   ├── theme-demo/     # 主题演示站（展示主题效果）
│   └── my-blog/        # 你的个人博客实例
├── package.json        # 根工作区配置
└── pnpm-workspace.yaml # 工作区配置
```

- **`packages/theme`**: 可复用的主题包，包含所有组件、样式和配置
- **`apps/theme-demo`**: 展示主题功能的演示站点（已部署在 [https://astro-md-theme-demo.netlify.app/](https://astro-md-theme-demo.netlify.app/)）
- **`apps/my-blog`**: 使用该主题的个人博客实例

## 🚀 快速开始

### 前置要求

- Node.js 18+
- pnpm 9+

### 安装依赖

```bash
pnpm install
```

### 开发模式

启动所有应用：
```bash
pnpm dev
```

单独启动演示站：
```bash
pnpm dev:demo
```

单独启动个人博客：
```bash
pnpm dev:blog
```

### 构建

构建所有应用：
```bash
pnpm build
```

## 📝 使用说明

### 主题包 (`packages/theme`)

这是核心主题包，包含：
- Astro 组件（Layout、Post、Header 等）
- Tailwind CSS 配置
- 全局样式和工具
- 页面模板（HomePage、PostDetailPage 等）

**修改主题时**：直接编辑 `packages/theme` 中的文件，所有使用该主题的应用都会自动热更新（得益于 workspace 设置）。

### 个人博客 (`apps/my-blog`)

你的博客内容以 Markdown 文件形式存放在 `apps/my-blog/src/content/blog/` 目录下。

**添加新文章**：
1. 在 `src/content/blog/` 目录下创建 `.md` 文件
2. 在文件头部添加 frontmatter：

```markdown
---
title: 文章标题
excerpt: 文章描述
pubDate: 2024-01-01
category: 分类名称
readingTime: 5 mins
coverImage: https://example.com/image.jpg
coverImageAlt: 图片描述
---

文章内容...
```

### 主题演示站 (`apps/theme-demo`)

用于展示主题的默认效果，方便预览和测试。

**在线演示**：[https://astro-md-theme-demo.netlify.app/](https://astro-md-theme-demo.netlify.app/)

## 🔧 开发工作流

1. **开发主题**：在 `packages/theme` 中修改组件和样式
2. **测试效果**：在 `apps/theme-demo` 中查看效果（运行在 `http://localhost:4321`）
3. **写博客**：在 `apps/my-blog` 中编写 Markdown 文章

所有修改都会实时反映，无需手动同步（得益于 workspace 设置）。

## 🚢 部署

### 部署演示站点

演示站点（`apps/theme-demo`）已配置为 Netlify 部署。请查看 `netlify.toml` 配置文件了解构建设置。

**在线演示**：[https://astro-md-theme-demo.netlify.app/](https://astro-md-theme-demo.netlify.app/)

### 部署你的博客

你可以将 `apps/my-blog` 部署到任何静态托管服务（Netlify、Vercel、GitHub Pages 等）：

1. 构建博客：`pnpm --filter my-blog build`
2. 部署 `apps/my-blog/dist` 目录

## 🔄 维护

### 更新依赖

更新所有依赖：
```bash
pnpm update
```

更新特定包：
```bash
pnpm update <package-name> --filter <app-name>
```

### 更新主题

由于 `apps/my-blog` 使用主题作为 workspace 依赖，主题更新会自动可用。只需拉取最新更改并重启开发服务器即可。

## 📦 发布主题

当你准备发布主题时，可以：

1. **发布到 NPM**：将 `packages/theme` 单独发布到 NPM 作为 `@astro-md-theme/core`
2. **作为模板提供**：将 `apps/theme-demo` 作为其他用户的起始模板

## 🎨 特性

- ✨ 现代、简约的设计
- 📱 完全响应式
- 🌓 暗色模式支持
- 📝 丰富的 Markdown 支持（表格、代码块、数学公式、脚注、定义列表）
- 🎯 TypeScript 类型安全
- ⚡ 使用 Astro 快速静态站点生成
- 🎨 使用 Tailwind CSS 可自定义

---

[English Documentation](README.md)
