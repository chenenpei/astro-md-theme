# My Blog

使用 Astro MD Theme 的个人博客应用。

## 📝 内容管理

### 博客文章结构

博客文章以 Markdown 文件形式存放在 `src/content/blog/` 目录下。每篇文章应遵循以下结构：

### 添加新文章

1. 在 `src/content/blog/` 目录下创建新的 `.md` 文件
2. 在文件头部添加 frontmatter：

```markdown
---
title: 文章标题
excerpt: 文章的简短描述
pubDate: 2024-01-15
category: 分类名称
readingTime: 5 mins
coverImage: https://example.com/image.jpg
coverImageAlt: 图片描述
---

你的文章内容...
```

### Frontmatter 字段

- **`title`** (必需): 博客文章的标题
- **`excerpt`** (可选): 在文章列表中显示的简短描述
- **`pubDate`** (必需): 发布日期，格式为 `YYYY-MM-DD`
- **`category`** (可选): 分类名称（例如："技术"、"摄影"、"写作"）
- **`readingTime`** (可选): 预计阅读时间（例如："5 mins"）
- **`coverImage`** (可选): 封面图片的 URL
- **`coverImageAlt`** (可选): 封面图片的替代文本

### 示例文章

```markdown
---
title: Astro 入门指南
excerpt: 学习如何使用 Astro 构建快速的静态站点
pubDate: 2024-01-15
category: 技术
readingTime: 10 mins
coverImage: https://images.unsplash.com/photo-1455390582262-044cdead277a?w=800&h=400&fit=crop
coverImageAlt: Astro 标志
---

# Astro 入门指南

这是我的第一篇关于 Astro 的博客文章...
```

## 🔄 更新主题

由于此博客使用 `@chenenpei/astro-md-theme` 作为 workspace 依赖，主题更新会自动可用：

1. **拉取最新更改**：从仓库拉取最新代码
2. **更新主题包**（如果单独发布）：
   ```bash
   pnpm update @chenenpei/astro-md-theme
   ```
3. **重启开发服务器**以查看更改：
   ```bash
   pnpm dev:blog
   ```

### 手动更新主题

如果你在 monorepo 中工作并想测试主题更改：

1. 在 `packages/theme/` 中进行更改
2. 更改会自动在你的博客中热更新（得益于 workspace 设置）
3. 无需手动同步！

## 🚀 开发

### 启动开发服务器

从根目录：
```bash
pnpm dev:blog
```

或从此目录：
```bash
pnpm dev
```

博客将在 `http://localhost:4321` 可用

### 构建生产版本

从根目录：
```bash
pnpm --filter my-blog build
```

或从此目录：
```bash
pnpm build
```

构建的站点将在 `dist/` 目录中。

## 🚢 部署

你可以将此博客部署到任何静态托管服务：

### Netlify

1. 构建命令：`pnpm --filter my-blog build`
2. 发布目录：`apps/my-blog/dist`

### Vercel

1. 构建命令：`pnpm --filter my-blog build`
2. 输出目录：`apps/my-blog/dist`

### GitHub Pages

1. 构建站点：`pnpm --filter my-blog build`
2. 将 `dist/` 目录部署到 GitHub Pages

## 📚 内容集合

此博客使用 Astro 的内容集合功能。架构定义在 `src/content/config.ts` 中。确保你的文章符合预期的架构。

## 🎨 自定义

要自定义博客外观：

1. **覆盖 CSS 变量**：在你自己的 CSS 文件中
2. **修改页面模板**：如果需要
3. **更新站点元数据**：在页面组件中（标题、描述等）

## 📖 参考

- **主题文档**：参见 `packages/theme/README.md`
- **演示站点**：[https://astro-md-theme-demo.netlify.app/](https://astro-md-theme-demo.netlify.app/)

---

[English Documentation](README.md)
